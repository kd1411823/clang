#include <stdio.h>
void Str_Reverse(char* s1, char* s2);

main()
{
	char str1[256], str2[256];
	printf("•¶š—ñ?:");
	gets(str1);
	Str_Reverse = (str1, str2);
	puts("str1:%s\n",str1);
	printf("str2:%s\n", str2);
}
void Str_Reverse(char* s1, char* s2) {
	int cut,i;
	for (cut = 0; *s1 != '\0'; cut++);
	return cut;
	*s2 = '\0'
		while (*s2 += *s1);
	return *s2;
}
